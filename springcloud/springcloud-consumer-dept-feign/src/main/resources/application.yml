server:
  port: 8002

eureka:
  client:
    register-with-eureka: false # 不向服务中注册自己
    service-url:
      defaultZone:  http://localhost:7001/eureka/,http://localhost:7002/eureka/,http://localhost:7003/eureka/
# 开启服务降级降级
feign:
  hystrix:
    enabled: true